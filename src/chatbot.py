"""Fase 6 — Chatbot.

Loop de conversación por consola que usa el agente. Maneja el identificador de
conversación (``thread_id``) y de usuario (``user_id``) para soportar múltiples
sesiones. El chatbot es multilingüe: se puede preguntar en español o inglés.
"""
# from src.agente import construir_agente


def conversar(user_id: str, thread_id: str):
    """Inicia un loop de chat para una conversación identificada."""
    # TODO Fase 6: loop de conversación con el agente
    raise NotImplementedError
