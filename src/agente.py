"""Fase 4 — Agente (LangGraph).

Construye el grafo del agente a mano: estado, nodo router (clasifica la consulta
en documentos / general / small-talk), nodos de herramientas y nodo de generación
con control de alucinaciones.

IMPORTANTE: prohibido usar create_agent() / create_react_agent() ni abstracciones
de alto nivel que oculten la lógica del agente (ver PLAN.md).
"""
# from langgraph.graph import StateGraph, START, END
# from langchain_groq import ChatGroq
# from src import config


def construir_agente():
    """Arma y compila el grafo de LangGraph del agente."""
    # TODO Fase 4: definir StateGraph, nodos (router, tools, generación) y edges
    raise NotImplementedError
